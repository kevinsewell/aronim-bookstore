apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    app: nginx
  ports:
    - name: http
      port: 80
      nodePort: 30004
    - name: https
      port: 443
      nodePort: 30005
  type: NodePort
