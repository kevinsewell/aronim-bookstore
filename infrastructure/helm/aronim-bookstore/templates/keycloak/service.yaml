apiVersion: v1
kind: Service
metadata:
  name: keycloak
spec:
  selector:
    app: keycloak
  ports:
    - name: default
      port: 8080
      nodePort: 30002
    - name: admin
      port: 9000
      nodePort: 30003
  type: NodePort
