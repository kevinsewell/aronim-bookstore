aronim:
  openapi:
    dev-url: https://bookstore.aronim.local
    prod-url: https://bookstore.aronim.com
logging:
  level:
    com.aronim.bookstore: DEBUG
server:
  port: 8081
spring:
  datasource:
    driver-class-name: org.h2.Driver
    password: password
    url: jdbc:h2:mem:bookstoredb
    username: sa
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0
    out-of-order: false
    validate-on-migrate: true
    placeholder-replacement: true
    placeholders:
      table_suffix: ${ENVIRONMENT:dev}
    schemas: public
    default-schema: public
    connect-retries: 3
    sql-migration-prefix: V
    repeatable-sql-migration-prefix: R
    sql-migration-separator: __
    sql-migration-suffixes: .sql
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: aronim-bookstore-backend-application
            client-secret: ${KEYCLOAK_CLIENT_SECRET:UY3j5TmSQIdBv1ke3Q8IlKPdrQ2jL4PU}
            scope: openid,profile,email
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
        provider:
          keycloak:
            issuer-uri: https://identity.aronim.local/realms/bookstore
            user-name-attribute: preferred_username
      resourceserver:
        jwt:
          issuer-uri: https://identity.aronim.local/realms/bookstore
          jwk-set-uri: https://identity.aronim.local/realms/bookstore/protocol/openid-connect/certs
    user:
      name: admin@localhost.local
      password: changeme
springdoc:
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: v1
      paths-to-match: /api/v1/**
  packages-to-scan: com.aronim.bookstore.catalog.api.controller
  swagger-ui:
    path: /swagger-ui.html
    display-request-duration: true
    groups-order: DESC
    operationsSorter: method
    disable-swagger-default-url: true
  show-actuator: false
